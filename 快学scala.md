2017.07.11
1、Scala解释器读到一个表达式，求值，然后将其打印出来，之后去读取下一个表达式，这个过程称之为读取-求值-打印-循环（）REPL。
   读取的内容被快速编译成字节码、并将字节码交由Java虚拟机执行。
2、在scala中，如果有变量或函数的类型，则将类型放在变量或函数后面，如：val str:String="caixiang"
   在scala中，当且仅当同一行存在多条语句时才需要用分号隔开，否则不需要使用分号。
3、在scala中，没有++和--，需要使用+=1和-=1
4、在使用以scala.开头的包时，可以省去scala前缀。
5、scala没有静态方法，但有个单例对象的特性来代替。
6、“hello”(3)表示字母‘l’

1、if表达式有值
2、void的类型为Unit
3、函数式定义时有=号
4、在Scala中，{}块中包含了一系列表达式，其结果也是一个表达式，快中最后一个表达式的值就是块的值。
5、赋值语句的值是Unit类型的。
6、如果for循环的循环体以yield开始，则该循环会构造出一个集合
   如：for(i<- 1 to 10) yield i%3    //生成vector(1,2,0,1,2,0,1,2,0,1)

2017.07.12
1、若长度固定使用Array,若长度可变则使用ArrayBuffer。
2、提供初始值时不用new。
3、数组通过()来访问元素而不是[]。
4、Scala数组和Java数组可以互换，使用ArrayBuffer声明，调用scala.collection.JavaConversions中的转换函数。

2017.07.13
1、